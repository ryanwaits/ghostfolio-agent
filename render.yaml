services:
  - type: web
    name: ghostfolio
    runtime: docker
    plan: standard
    region: oregon
    healthCheckPath: /api/v1/health
    envVars:
      - key: ACCESS_TOKEN_SALT
        generateValue: true
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: ghostfolio-db
          property: connectionString
      - key: REDIS_HOST
        fromService:
          name: ghostfolio-redis
          type: redis
          property: host
      - key: REDIS_PORT
        fromService:
          name: ghostfolio-redis
          type: redis
          property: port
      - key: REDIS_PASSWORD
        value: ""
      - key: POSTGRES_USER
        fromDatabase:
          name: ghostfolio-db
          property: user
      - key: POSTGRES_PASSWORD
        fromDatabase:
          name: ghostfolio-db
          property: password
      - key: POSTGRES_DB
        fromDatabase:
          name: ghostfolio-db
          property: database
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: API_KEY_FINANCIAL_MODELING_PREP
        sync: false
      - key: DATA_SOURCES
        value: '["FINANCIAL_MODELING_PREP","COINGECKO","MANUAL"]'
      - key: DATA_SOURCE_EXCHANGE_RATES
        value: FINANCIAL_MODELING_PREP
      - key: DATA_SOURCE_IMPORT
        value: FINANCIAL_MODELING_PREP
      - key: NODE_ENV
        value: production

  - type: redis
    name: ghostfolio-redis
    plan: standard
    region: oregon
    maxmemoryPolicy: volatile-lru
    ipAllowList: []

databases:
  - name: ghostfolio-db
    plan: basic-1gb
    region: oregon
    databaseName: ghostfolio
    user: ghostfolio
